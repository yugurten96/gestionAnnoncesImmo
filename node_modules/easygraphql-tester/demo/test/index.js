'use strict'

const gql = require('graphql-tag')

const EasyGraphQLTester = require('../../lib')
const typeDefs = require('./schema.js'); 

let tester = new EasyGraphQLTester(typeDefs);

const query = `
  {
    getCurrentUser {
      username
    }
  }
`

console.log(tester.mock(query))