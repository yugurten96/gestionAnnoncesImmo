'use strict' 

const schema = `
  type Query {
    hello: String!
  }
`

function hello(root, args, ctx) {
  return 'Hello World!'
}

const resolvers = {
  Query: {
    hello    
  }
}

module.exports = { schema, resolvers }